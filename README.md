# SecureGate API

SecureGate API is a backend service for generating, managing, and securing API keys.
It provides authentication, API key hashing, and rate-limited access to protected endpoints.

## Features
- JWT-based user authentication
- API key generation & revocation
- Hashed API key storage
- Rate limiting per key
- Protected endpoints using API keys